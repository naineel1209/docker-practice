1. ec2
2. vpc - virtual private cloud
 - security group
 - cidr block
 - subnet - public and private
    - internet gateway
3. ebs - elastic block storage - volume - snapshot (backup)
    - ebs is used as a storage for the ec2 instance
    - ebs is a network drive that can be attached to the ec2 instance
    - ebs volume is placed in a specific availability zone
    - ebs volume is automatically replicated within that availability zone
    - ebs volume can be detached from one instance and attached to another instance quickly
4. s3 storage - scalable storage service - buckets
    - s3 is a object storage
    - s3 is a universal namespace, that is, names must be unique
    - encryption is enabled by default
5. IAM - identity and access management
6. Cloudwatch - monitoring service
7. Lambda - serverless computing
    - lambda is a compute service that lets you run code without provisioning or managing servers
    - just deploy the code and lambda takes care of everything
    - lambda is charged based on the number of requests
8. Cloud Build Service - code build
    - code build is a fully managed build service that compiles source code, runs tests, and produces software packages that are ready to deploy
    - its competitors are jenkins, travis ci, circle ci, Github actions, Gitlab CI
    - AWS CodePipeline is a continuous integration and continuous delivery service for fast and reliable application and infrastructure updates
    - AWS CodeBuild scales continuously and processes multiple builds concurrently
    - AWS CodeDeploy automates code deployments to any instance, including Amazon EC2 instances and instances running on-premises
9. AWS Configuration
10. Billing And Costing
11. KMS - key management service
    - KMS is a managed service that makes it easy for you to create and control the encryption keys used to encrypt your data
    - KMS is integrated with most other AWS services
12. CloudTrail - logging service
13. CloudFormation - infrastructure as code
14. Elastic Beanstalk - deploy web applications
15. RDS - relational database service
16. EKS - elastic kubernetes service
    - EKS is a managed service that makes it easy for you to run Kubernetes on AWS without needing to install and operate your own Kubernetes clusters
    - EKS runs the Kubernetes management infrastructure for you across multiple AWS availability zones to eliminate a single point of failure
17. ECS - elastic container service
    - ECS is a fully managed container orchestration service
    - ECS is a highly scalable, high-performance container management service that supports Docker containers
    - ECS eliminates the need for you to install, operate, and scale your own cluster management infrastructure
18. ELK - elastic search, logstash, kibana
    - ELK is a log management platform
    - ELK is a combination of three open-source tools: Elasticsearch, Logstash, and Kibana
    - Elasticsearch is a search and analytics engine
    - Logstash is a server-side data processing pipeline that ingests data from multiple sources simultaneously, transforms it, and then sends it to a "stash" like Elasticsearch
    - Kibana lets you visualize your Elasticsearch data and navigate the Elastic Stack
19. SNS - simple notification service
20. SQS - simple queue service
