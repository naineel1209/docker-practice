echo 'naineel' | cat
ls
# ll
ls -a
# lsof
pwd

#creating a new file
touch newfile

#vim = create a file and write into file
# vi newfile2

#nproc  - cpus used by the ubuntu
nproc

free -m
free -g

df -h #human readable format of the all the disk storage space of the files on the os

chmod a+rwx intro.sh

chmod --help